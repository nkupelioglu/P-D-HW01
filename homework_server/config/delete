service delete
{
        port = 3003
        disable = no
        socket_type = stream
        protocol = tcp
        wait = no
        user = deneme1
        server = /home/deneme1/Desktop/py_app/delete
        log_on_failure += USERID
        log_on_success += USERID
}

